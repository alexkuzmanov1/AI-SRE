// Phase-4 replaces this body with the real Anthropic agent loop.
// Signature stays the same so it drops in without touching the controller.
export function startInvestigation(incidentId: string): void {
  console.log(`[stub] would investigate ${incidentId}`);
}
